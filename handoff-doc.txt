I need you to build a Gift Finder web application. Here are the complete specifications:

PROJECT SETUP:
- Create a Next.js 14 app with TypeScript
- Use Tailwind CSS for styling
- Include lucide-react for icons
- Set up environment variables for Anthropic API key
- Configure for deployment on Vercel

APPLICATION STRUCTURE:
The app has 3 main steps in a multi-step form:

STEP 1 - Basic Information:
- Relationship selector (14 options with emojis): Parent, Sibling, Child, Grandparent, Aunt/Uncle, Cousin, Friend, Best Friend, Colleague, Boss, Partner, Teacher, Neighbor, Client
- Location input with autocomplete (use mock data or integrate Google Places API)
- Budget selector (6 button options): Under $25, $25-$50, $50-$100, $100-$250, $250-$500, $500+
- Gift type selector (3 options): Tangible Gift, Experience, Open to Both
- Occasion selector (10 preset buttons + custom option): Birthday, Anniversary, Wedding, Graduation, Holiday, Thank You, Congratulations, Housewarming, New Baby, Just Because

STEP 2 - Personalization:
- Age range dropdown: Child (0-12), Teen (13-17), Young Adult (18-29), Adult (30-49), Middle Age (50-64), Senior (65+)
- Interests multi-select (12 options): Books & Reading, Cooking, Sports, Technology, Art & Crafts, Music, Travel, Fashion, Gaming, Fitness, Gardening, Photography
- Personality traits multi-select (9 options): Adventurous, Homebody, Creative, Practical, Loves Luxury, Minimalist, Outdoorsy, Tech Savvy, Foodie

STEP 3 - Recommendations Display:
- Show loading state with animation while generating
- Display 6 AI-generated gift cards in a 2-column grid
- Each card shows: emoji, gift name, price, description, shop name, "View & Purchase" button
- Include error handling and fallback to mock data if API fails

DESIGN REQUIREMENTS:
- Gradient background: purple-50 via pink-50 to blue-50
- Purple accent color (#9333ea) for primary actions
- All form inputs should be large, clickable buttons (minimize typing)
- Progress bar showing current step (1/2/3)
- Responsive design (mobile-first)
- Smooth transitions and hover states

CLAUDE API INTEGRATION:
- Use Anthropic's API endpoint: https://api.anthropic.com/v1/messages
- Model: claude-sonnet-4-20250514
- Construct a detailed prompt including all user inputs
- Request JSON response with 6 gift objects containing: name, price, type, description, shop, category, emoji
- Handle response parsing (strip markdown code blocks if present)
- Implement proper error handling

API PROMPT STRUCTURE:
"You are a gift recommendation expert. Based on the following information, suggest 6 thoughtful and personalized gift ideas...
[Include all form data: relationship, location, budget, gift type, occasion, age, interests, personality]
Please respond ONLY with a JSON array of exactly 6 gift recommendations with this structure: {name, price, type, description, shop, category, emoji}"

TECHNICAL REQUIREMENTS:
- Store API key in .env.local as ANTHROPIC_API_KEY
- Create API route in /app/api/generate-gifts/route.ts for API calls
- Use React hooks: useState for form state, loading states
- Implement form validation (disable Next button until required fields filled)
- Add "Start Over" functionality to reset form

FILE STRUCTURE:
/app
  /page.tsx (main component)
  /api
    /generate-gifts
      /route.ts (API endpoint)
/components
  /GiftFinder.tsx (main form component)
  /StepIndicator.tsx (progress bar)
  /GiftCard.tsx (individual gift display)
/types
  /index.ts (TypeScript interfaces)
/lib
  /constants.ts (all dropdown options and data)

DEPLOYMENT:
- Configure for Vercel deployment
- Include vercel.json if needed
- Set up environment variable instructions in README

Create a comprehensive, production-ready application following these specifications.
